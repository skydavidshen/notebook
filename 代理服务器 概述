1：标准的代理服务器 

内网用户 -> 正向代理服务器 -> 外网 

ps : 内网的用户需要在自己的浏览器上设置代理服务器的相关设置，代理服务器可以是内网的，主要是缓存一些访问过得数据，资源。当被
缓存的页面第二次访问的时候就直接从本地的代理服务器那里请求数据而不再向源web网站请求数据，节省了宝贵的带宽，提高了访问速度
。

2：透明代理服务器

内网用户 -> 正向代理服务器 -> 外网 

ps : 内网的用户需要在自己的浏览器上不需要做任何设置，只需要使用iptables或是ipchains实现。其他的原理是一样的。

3：反向代理

和其他的两种代理是完全不一样的，一个是内网访问外网，而这个是外网用户访问内网。

外网用户 -> 反向代理服务器（在内网) -> 内网web服务器（原始的web服务器）

可以降低原始web服务器的负载，


